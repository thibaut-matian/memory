* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Poppins', sans-serif;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    min-height: 100vh;
    color: #333;
}

.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
}

/* Header */
.header {
    text-align: center;
    margin-bottom: 30px;
}

.header h1 {
    color: white;
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 20px;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
}

.nav {
    display: flex;
    justify-content: center;
    gap: 20px;
}

.nav-link {
    color: white;
    text-decoration: none;
    padding: 10px 20px;
    border-radius: 25px;
    transition: all 0.3s ease;
    font-weight: 500;
}

.nav-link:hover,
.nav-link.active {
    background: rgba(255,255,255,0.2);
    backdrop-filter: blur(10px);
}

/* Auth Section */
.auth-section {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 50vh;
}

.auth-card {
    background: white;
    padding: 40px;
    border-radius: 20px;
    box-shadow: 0 15px 35px rgba(0,0,0,0.1);
    text-align: center;
    max-width: 400px;
    width: 100%;
}

.auth-card h2 {
    margin-bottom: 30px;
    color: #333;
}

.auth-form {
    display: flex;
    flex-direction: column;
    gap: 15px;
}

.auth-form input {
    padding: 15px;
    border: 2px solid #e1e5e9;
    border-radius: 10px;
    font-size: 16px;
    transition: border-color 0.3s ease;
}

.auth-form input:focus {
    outline: none;
    border-color: #667eea;
}

.auth-buttons {
    display: flex;
    gap: 10px;
    margin-top: 20px;
}

/* Buttons */
.btn {
    padding: 12px 24px;
    border: none;
    border-radius: 10px;
    cursor: pointer;
    font-size: 16px;
    font-weight: 500;
    transition: all 0.3s ease;
    text-decoration: none;
    display: inline-block;
    text-align: center;
}

.btn-primary {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
}

.btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
}

.btn-secondary {
    background: #f8f9fa;
    color: #333;
    border: 2px solid #e1e5e9;
}

.btn-secondary:hover {
    background: #e9ecef;
}

.btn-small {
    padding: 8px 16px;
    font-size: 14px;
}

/* Game Section */
.game-section {
    max-width: 1000px;
    margin: 0 auto;
}

.game-controls {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: white;
    padding: 20px;
    border-radius: 15px;
    margin-bottom: 20px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    flex-wrap: wrap;
    gap: 15px;
}

.difficulty-selector label {
    margin-right: 10px;
    font-weight: 500;
}

.difficulty-selector select {
    padding: 10px;
    border: 2px solid #e1e5e9;
    border-radius: 8px;
    font-size: 16px;
}

.player-info {
    display: flex;
    align-items: center;
    gap: 15px;
}

.game-stats {
    display: flex;
    justify-content: center;
    gap: 30px;
    background: white;
    padding: 20px;
    border-radius: 15px;
    margin-bottom: 30px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    flex-wrap: wrap;
}

.stat {
    text-align: center;
}

.stat-label {
    display: block;
    font-weight: 500;
    color: #666;
    margin-bottom: 5px;
}

.stat span:last-child {
    font-size: 1.5rem;
    font-weight: 600;
    color: #333;
}

/* Game Board */
.game-board {
    display: grid;
    gap: 15px;
    justify-content: center;
    margin: 30px 0;
    padding: 20px;
}

.game-board.grid-3 { grid-template-columns: repeat(3, 1fr); max-width: 540px; margin: 30px auto; }
.game-board.grid-4 { grid-template-columns: repeat(4, 1fr); max-width: 720px; margin: 30px auto; }
.game-board.grid-5 { grid-template-columns: repeat(5, 1fr); max-width: 900px; margin: 30px auto; }
.game-board.grid-6 { grid-template-columns: repeat(6, 1fr); max-width: 1080px; margin: 30px auto; }

.card {
    aspect-ratio: 1;
    background: white;
    border-radius: 15px;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2.75rem;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    min-height: 160px;
    user-select: none;
}

.card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 25px rgba(0,0,0,0.2);
}

.card.flipped {
    animation: flipCard 0.6s ease-in-out;
}

.card.matched {
    opacity: 0.7;
    cursor: default;
    transform: scale(0.95);
}

@keyframes flipCard {
    0% { transform: rotateY(0deg); }
    50% { transform: rotateY(90deg); }
    100% { transform: rotateY(0deg); }
}

/* Messages */
.message {
    padding: 15px 20px;
    margin: 20px 0;
    border-radius: 10px;
    text-align: center;
    font-weight: 500;
}

.message.success {
    background: #d4edda;
    color: #155724;
    border: 1px solid #c3e6cb;
}

.message.error {
    background: #f8d7da;
    color: #721c24;
    border: 1px solid #f5c6cb;
}

/* Game Board - Cartes */
.card-button {
    width: 100%;
    height: 100%;
    border: none;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    font-size: 2.25rem;
    font-weight: bold;
    border-radius: 15px;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
}

.card-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
}

.card-button:active {
    transform: translateY(0);
}

/* Game Completed Static */
.game-completed-static {
    background: rgba(255,255,255,0.95);
    padding: 30px;
    border-radius: 20px;
    margin: 30px 0;
    text-align: center;
    box-shadow: 0 10px 30px rgba(0,0,0,0.2);
    border: 3px solid #667eea;
}

.game-completed-static .completion-card {
    background: none;
    padding: 0;
    box-shadow: none;
}

/* No Game Message */
.no-game {
    text-align: center;
    padding: 60px 20px;
    color: #666;
    font-size: 1.2rem;
}

.no-game p {
    margin: 0;
}

/* Leaderboard */
.leaderboard-section {
    background: white;
    padding: 30px;
    border-radius: 20px;
    box-shadow: 0 15px 35px rgba(0,0,0,0.1);
}

.leaderboard-section h2 {
    text-align: center;
    margin-bottom: 30px;
    color: #333;
}

.leaderboard-controls {
    text-align: center;
    margin-bottom: 30px;
}

.leaderboard {
    background: #f8f9fa;
    border-radius: 15px;
    overflow: hidden;
}

.leaderboard-header {
    display: grid;
    grid-template-columns: 60px 1fr 100px 80px 80px 80px 120px;
    gap: 15px;
    padding: 20px;
    background: #667eea;
    color: white;
    font-weight: 600;
}

.leaderboard-row {
    display: grid;
    grid-template-columns: 60px 1fr 100px 80px 80px 80px 120px;
    gap: 15px;
    padding: 15px 20px;
    border-bottom: 1px solid #e1e5e9;
    align-items: center;
}

.leaderboard-row:nth-child(even) {
    background: white;
}

.leaderboard-row:first-child {
    background: #ffd700;
    font-weight: 600;
}

.leaderboard-row:nth-child(2) {
    background: #c0c0c0;
    font-weight: 600;
}

.leaderboard-row:nth-child(3) {
    background: #cd7f32;
    font-weight: 600;
}

.leaderboard-info {
    margin-top: 30px;
    padding: 20px;
    background: #f8f9fa;
    border-radius: 10px;
}

.leaderboard-info ul {
    margin-left: 20px;
    margin-top: 10px;
}

/* Profile */
.profile-section {
    background: white;
    padding: 30px;
    border-radius: 20px;
    box-shadow: 0 15px 35px rgba(0,0,0,0.1);
}

.profile-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 30px;
}

.profile-header h2 {
    color: #333;
}

.stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 20px;
    margin-bottom: 40px;
}

.stat-card {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 25px;
    border-radius: 15px;
    text-align: center;
    box-shadow: 0 5px 15px rgba(102, 126, 234, 0.3);
}

.stat-card h3 {
    margin-bottom: 15px;
    font-size: 1rem;
    opacity: 0.9;
}

.stat-value {
    font-size: 2rem;
    font-weight: 700;
}

.profile-history {
    margin-bottom: 40px;
}

.profile-history h3 {
    margin-bottom: 20px;
    color: #333;
}

.history-controls {
    margin-bottom: 20px;
}

.history-table {
    background: #f8f9fa;
    border-radius: 15px;
    overflow: hidden;
}

.history-header {
    display: grid;
    grid-template-columns: 80px 100px 80px 80px 120px;
    gap: 15px;
    padding: 20px;
    background: #667eea;
    color: white;
    font-weight: 600;
}

.history-row {
    display: grid;
    grid-template-columns: 80px 100px 80px 80px 120px;
    gap: 15px;
    padding: 15px 20px;
    border-bottom: 1px solid #e1e5e9;
    align-items: center;
}

.history-row:nth-child(even) {
    background: white;
}

.achievements-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 15px;
}

.achievement {
    padding: 20px;
    border-radius: 10px;
    text-align: center;
    border: 2px solid #e1e5e9;
    transition: all 0.3s ease;
}

.achievement.unlocked {
    background: linear-gradient(135deg, #2ecc71 0%, #27ae60 100%);
    color: white;
    border-color: #27ae60;
}

.achievement.locked {
    background: #f8f9fa;
    color: #999;
    opacity: 0.6;
}

.achievement-icon {
    font-size: 2rem;
    margin-bottom: 10px;
}

.achievement-title {
    font-weight: 600;
    margin-bottom: 5px;
}

.achievement-desc {
    font-size: 0.9rem;
}

/* Responsive */
@media (max-width: 768px) {
    .container {
        padding: 10px;
    }
    
    .header h1 {
        font-size: 2rem;
    }
    
    .nav {
        flex-wrap: wrap;
        gap: 10px;
    }
    
    .game-controls {
        flex-direction: column;
        text-align: center;
    }
    
    .game-stats {
        gap: 15px;
    }
    
    .game-board {
        gap: 10px;
        padding: 10px;
    }
    
    .card {
        font-size: 1.5rem;
        min-height: 60px;
    }
    
    .leaderboard-header,
    .leaderboard-row {
        grid-template-columns: 40px 1fr 80px 60px 60px 60px 100px;
        gap: 10px;
        padding: 15px 10px;
        font-size: 0.9rem;
    }
    
    .history-header,
    .history-row {
        grid-template-columns: 60px 80px 60px 60px 100px;
        gap: 10px;
        padding: 15px 10px;
        font-size: 0.9rem;
    }
    
    .stats-grid {
        grid-template-columns: 1fr;
    }
    
    .auth-buttons {
        flex-direction: column;
    }
}

/* --- Liquid/Glassmorphism Enhancements (overrides) --- */
/* We keep the existing purple gradient, and add subtle glass/liquid effects */

.auth-card,
.game-controls,
.game-stats {
    background: rgba(255,255,255,0.15);
    backdrop-filter: blur(10px) saturate(140%);
    -webkit-backdrop-filter: blur(10px) saturate(140%);
    border: 1px solid rgba(255,255,255,0.25);
}

.auth-card h2 { color: #fff; }
.auth-form input {
    border: 1px solid rgba(255,255,255,0.4);
    background: rgba(255,255,255,0.2);
    color: #fff;
}
.auth-form input::placeholder { color: rgba(255,255,255,0.85); }
.auth-form input:focus {
    border-color: #fff;
    background: rgba(255,255,255,0.3);
}

.difficulty-selector select {
    border: 1px solid rgba(255,255,255,0.4);
    background: rgba(255,255,255,0.2);
    color: #fff;
}
.difficulty-selector select option { color: #333; }

.card {
    background: rgba(255,255,255,0.18);
    border: 1px solid rgba(255,255,255,0.25);
    box-shadow: 0 12px 28px rgba(0,0,0,0.22);
    transition: transform 0.25s ease, box-shadow 0.25s ease;
}
.card:hover { transform: translateY(-6px); }
.card.matched { background: rgba(255,255,255,0.28); opacity: 0.9; }

.card-button {
    /* Keep gradient */
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    font-weight: 700;
    border-radius: 16px;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
}
.card-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 22px rgba(102,126,234,0.45);
}

.message.success {
    background: rgba(46,204,113,0.2);
    color: #ecf9f1;
    border: 1px solid rgba(46,204,113,0.45);
}
.message.error {
    background: rgba(231,76,60,0.25);
    color: #fdecea;
    border: 1px solid rgba(231,76,60,0.45);
}

/* Subtle floating animation for liquid feel */
.card, .auth-card, .game-controls, .game-stats {
    animation: floaty 6s ease-in-out infinite;
}
@keyframes floaty {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-4px); }
}

/* Card container and image styles for One Piece images */
.card-container {
    width: 100%;
    aspect-ratio: 1 / 1;
    perspective: 900px;
    display: flex;
    align-items: center;
    justify-content: center;
}

.card.face.front {
    color: #fff;
    font-weight: 600;
    transform: translateY(0) scale(1);
    /* ensure front face does not hide the image */
    background: transparent !important;
}

.card-img {
    width: 96%;
    height: 96%;
    object-fit: contain;
    display: block;
    border-radius: 10px;
}

.card.face.front img { 
    display: block; 
    width: 100%;
    height: 100%;
    object-fit: contain; 
}

/* make .game-board grid columns adapt to number of cards */
.game-board.grid-3 { grid-template-columns: repeat(3, 1fr); }
.game-board.grid-4 { grid-template-columns: repeat(4, 1fr); }
.game-board.grid-5 { grid-template-columns: repeat(5, 1fr); }
.game-board.grid-6 { grid-template-columns: repeat(6, 1fr); }

@media (max-width: 768px) {
    .card-img { width: 80%; height: 80%; }
    .card { min-height: 120px; font-size: 1.6rem; }
}
